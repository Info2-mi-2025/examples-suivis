#include "array.h"

int main(){
    // bonne solution
    PointArray* a = create_array(10);

    // plus simple
    //Array a;
    //create_array(&a, 10);

    // add_point(a, 1,2);
    // add_point(a, -1,2);
    // print_array(a);
}